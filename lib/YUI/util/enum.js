// 指令
const dirPrefix = "v-";
export const DIRECTIVE_TYPES = {
  MODEL: `${dirPrefix}model`,
  TEXT: `${dirPrefix}text`
};

// 节点类型
export const NODE_TYPES = {
  ELEMENT: 1,
  ATTRIBUTE: 2,
  TEXT: 3,
};
